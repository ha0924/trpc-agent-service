# 数据模型设计

本文说明平台的表清单与各表用途，并给出一期落地的表结构。

## 1. 全表清单

共 26 张，按职责分六组。★ 为已落地建表，共 12 张，DDL 见第 5 节。

### 1.1 租户与配置

| 表 | 用途 |
|---|---|
| ★ `tenants` | 租户本身，隔离边界。`settings` 装租户级策略：预算、限流、模型白名单、脱敏规则 |
| ★ `agent_apps` | 租户下有哪些 Agent，如客服、运维、数据分析 |
| ★ `agent_versions` | 某一版的 Prompt 与模型配置，发布后不可修改 |
| ★ `agent_deployments` | 当前环境使用哪个版本及灰度权重，回滚即修改此表 |

### 1.2 能力定义与授权

定义表跨版本共享，绑定表按版本冻结，随版本一同回滚。

| 表 | 用途 |
|---|---|
| `tool_definitions` | 平台提供哪些 Tool |
| ★ `agent_tool_bindings` | 某个版本可用哪些 Tool，取值 allow / deny / ask |
| `extension_definitions` | Plugin、Guardrail、Callback 的定义，由 `kind` 区分 |
| ★ `agent_extension_bindings` | 某个版本启用哪些扩展及其参数 |
| `mcp_servers` | 租户接入了哪些 MCP 服务 |
| ★ `agent_mcp_bindings` | 某个版本使用哪几个 MCP 服务 |
| `knowledge_bases` | 租户有哪些知识库，各指向哪个向量库 |
| `agent_knowledge_bindings` | 某个版本挂载哪几个知识库 |
| `skills` | 租户的 Skill 工作流 |
| ★ `agent_skill_bindings` | 某个版本挂载哪些 Skill |

Tool、扩展、MCP、Skill 四类能力配置以绑定表存放，不并入 `agent_versions` 的单列 JSON。这四类均为不定长列表且会持续增长，合并存储会使该列不断膨胀，而每次装配 Runtime 都需整列读出并解析。拆表带来的多次查询由 Runtime 缓存吸收——版本不可变，缓存不失效，查询只发生在首次装配。Prompt 与模型配置是一个版本恰好一份的定长配置，因此仍作为 `agent_versions` 的列。

Plugin、Guardrail、Callback 的实现在代码中注册，表中只记录启用项与参数，Runtime 装配时读表挂载。因此新增一种扩展只需注册实现，不改装配逻辑，也不改表结构。

### 1.3 通道与身份

| 表 | 用途 |
|---|---|
| ★ `channel_bindings` | IM 账号绑定到哪个租户的哪个 Agent，并记录通道能力描述符 |
| `channel_users` | 外部 IM 用户映射为内部用户 |

### 1.4 运行数据

| 表 | 用途 |
|---|---|
| ★ `sessions` | 会话元数据，固化创建时选定的 Agent 版本 |
| ★ `session_events` | 会话内的每条消息与每次 Tool 调用，带 `sequence` 保序 |
| ★ `inbound_events` | 入站幂等记录与执行、投递状态机，同时是在途请求的可靠记录 |
| `session_summaries` | 长会话的历史摘要 |
| `memories` | 跨会话长期记忆 |

### 1.5 后端路由

| 表 | 用途 |
|---|---|
| `backend_configs` | 平台接入了哪些后端实例，一行一个数据库或存储 |
| `backend_bindings` | 某类数据走哪个后端实例，即 Storage Router 的路由依据 |

拆为两张表的原因是同一个后端实例会被多个租户、多种数据类型共用。若合为一张，连接信息需在每条绑定中重复，且更换密钥要改动多行。拆开后 `backend_configs` 表达"有哪些后端"，`backend_bindings` 表达"谁用哪个"，新增租户只增加绑定记录。

### 1.6 审计与治理记录

运行时产生的记录，性质上不属于配置。

| 表 | 用途 |
|---|---|
| `tool_approvals` | 危险 Tool 的二次确认记录 |
| `audit_logs` | 谁、何时、经由哪个通道、使用哪个 Agent 或 Tool、系统允许还是拒绝 |
| `usage_records` | Token 与成本明细 |

### 1.6.1 audit_policies（已落地）

租户模型的第 7 要素。按租户配置审计里能留多少原文：

| 字段 | 含义 |
|---|---|
| `redact_level` | `none` / `standard` / `strict`。生产最低 `standard` |
| `body_mode` | `full` 留原文 / `truncate` 截断 / `hash` 只留摘要 / `drop` 不留 |
| `body_max_chars` | `truncate` 时的截断长度，按**字符**而非字节计 |
| `retention_days` | 保留天数 |

为什么必须按租户配置而非全局一套：不同租户的诉求相反。受监管行业要求留证据以便复查，注重隐私的租户要求正文一律不落库、只留哈希。写死任何一种都会让另一类租户无法使用。

三处设计取舍：

- **缺行时用安全默认而非最宽松。** 无策略的租户按 `truncate` 处理。若默认 `full`，这个功能一上线就会开始记录所有既有租户的完整正文。
- **凭据脱敏不受此策略影响。** 密钥无条件剥离，`redact_level=none` 也不例外——租户不能给自己开权限去泄漏平台的密钥。本策略只管用户产生的内容。
- **在 `WriteAudit` 单一收口处生效**，不在各调用点。靠约定执行的规则，总会在最关键的那个调用点被忘记。

### 1.7 不落表的两项

- **密钥**：明文保存在 Secret Manager，库中只存 `secret://` 形式的引用，分散于 `channel_bindings.secret_ref`、`backend_configs.dsn_ref` 与版本配置中；
- **预算计数器**：`usage_records` 是明细，用于对账。判断租户是否超额不能实时 `SUM` 明细，且多个 Worker 并发扣减需要原子操作，因此计数器放 Redis 按租户与周期累加。若缺少这一项，预算限制会退化为只统计不拦截。

## 2. 通用约定

- 物理主键统一为自增 `BIGINT UNSIGNED`，业务标识另立 `VARCHAR` 唯一键。业务标识用于跨进程传递与日志，不暴露自增值；
- 除 `tenants` 外所有表携带 `tenant_id`，且索引以 `tenant_id` 起头，保证租户维度查询走索引；
- 枚举值使用 `VARCHAR` 而非 `ENUM`，便于后续增加取值而不改表结构；
- 不使用外键约束，关联关系由应用层保证，避免后续分库与跨后端受限；
- 关键表预留 JSON 扩展列，新增可选属性不改表；
- 表示"租户级默认"时使用空串而非 `NULL`，因 MySQL 唯一键不约束 `NULL`；
- 字符集 `utf8mb4`，时间列统一 `DATETIME(3)`。

## 3. 与 Redis 的分工

`sessions` 表存会话的长期元数据：所属租户与 Agent、创建时固化的版本、外部标识映射、最后活跃时间。会话的短期上下文即 Session State 存 Redis，Session 租约与信箱同样存 Redis。

`session_events` 是全量历史，属体量最大的表，落 MySQL。

## 4. 扩展位置

| 扩展方向 | 位置 |
|---|---|
| 租户级预算与限流 | `tenants.settings` |
| Agent 模型调用参数 | `agent_versions.model_params` |
| Tool 的版本级覆盖配置 | `agent_tool_bindings.params` |
| 扩展参数 | `agent_extension_bindings.params` |
| 更精细的灰度规则 | `agent_deployments.strategy` |
| 通道能力差异 | `channel_bindings.capabilities` |
| 用户角色与权限判断 | `channel_users.attributes` |
| 新增数据类型与后端 | `backend_bindings.data_type` 取值 |

## 5. 核心表结构

以下为已落地的 12 张表的 DDL，覆盖四条链路：识别租户、定位会话、装配 Runtime、数据落库。其余 14 张表按第 1 节的用途说明设计，字段随实现补充。

### 5.1 tenants

```sql
CREATE TABLE tenants (
  id         BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  tenant_id  VARCHAR(64)  NOT NULL COMMENT '业务标识',
  name       VARCHAR(128) NOT NULL,
  status     VARCHAR(32)  NOT NULL DEFAULT 'active' COMMENT 'active / suspended',
  settings   JSON         NULL COMMENT '预留：预算、限流等租户级策略',
  created_at DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updated_at DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (id),
  UNIQUE KEY uk_tenant (tenant_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='租户';
```

### 5.2 agent_apps

```sql
CREATE TABLE agent_apps (
  id           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  tenant_id    VARCHAR(64)  NOT NULL,
  agent_app_id VARCHAR(64)  NOT NULL,
  name         VARCHAR(128) NOT NULL,
  description  VARCHAR(512) NULL,
  status       VARCHAR(32)  NOT NULL DEFAULT 'active',
  created_at   DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updated_at   DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (id),
  UNIQUE KEY uk_app (tenant_id, agent_app_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='Agent 应用';
```

### 5.3 agent_versions

已发布版本不可修改，配置变化时创建新版本。修改 Prompt、模型、Tool、扩展、MCP、Skill 均属配置变化，一律走新版本。

```sql
CREATE TABLE agent_versions (
  id                BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  tenant_id         VARCHAR(64)  NOT NULL,
  agent_app_id      VARCHAR(64)  NOT NULL,
  version           VARCHAR(64)  NOT NULL COMMENT '如 v1、v2',
  status            VARCHAR(32)  NOT NULL DEFAULT 'draft' COMMENT 'draft / published / archived',
  system_prompt     TEXT         NULL,
  model_name        VARCHAR(128) NOT NULL,
  model_api_key_ref VARCHAR(255) NULL COMMENT '密钥引用，不存明文',
  model_params      JSON         NULL COMMENT 'temperature、max_tokens 等调用参数',
  description       VARCHAR(512) NULL,
  published_at      DATETIME(3)  NULL,
  created_by        VARCHAR(64)  NULL,
  created_at        DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updated_at        DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (id),
  UNIQUE KEY uk_version (tenant_id, agent_app_id, version)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='Agent 版本配置';
```

模型配置作为列而非独立表，因其是「一个版本恰好一份」的定长配置，独立成表只增加装配时的查询次数。

### 5.4 agent_tool_bindings

```sql
CREATE TABLE agent_tool_bindings (
  id           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  tenant_id    VARCHAR(64)  NOT NULL,
  agent_app_id VARCHAR(64)  NOT NULL,
  version      VARCHAR(64)  NOT NULL,
  tool_name    VARCHAR(128) NOT NULL,
  mode         VARCHAR(16)  NOT NULL DEFAULT 'allow' COMMENT 'allow / deny / ask',
  params       JSON         NULL COMMENT '该版本对此 Tool 的超时、限流等覆盖配置',
  created_at   DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  PRIMARY KEY (id),
  UNIQUE KEY uk_tool (tenant_id, agent_app_id, version, tool_name),
  KEY idx_tool_name (tool_name) COMMENT '反查哪些版本在使用某个 Tool'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='版本可用的 Tool';
```

`mode` 为 `ask` 表示调用前需二次确认。`idx_tool_name` 支持反查影响面，用于 Tool 下线或安全排查。

### 5.5 agent_extension_bindings

```sql
CREATE TABLE agent_extension_bindings (
  id             BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  tenant_id      VARCHAR(64)  NOT NULL,
  agent_app_id   VARCHAR(64)  NOT NULL,
  version        VARCHAR(64)  NOT NULL,
  kind           VARCHAR(16)  NOT NULL COMMENT 'plugin / guardrail / callback',
  extension_name VARCHAR(128) NOT NULL,
  enabled        TINYINT      NOT NULL DEFAULT 1,
  priority       INT          NOT NULL DEFAULT 0 COMMENT '同类扩展的挂载顺序，值小者先执行',
  params         JSON         NULL,
  created_at     DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  PRIMARY KEY (id),
  UNIQUE KEY uk_ext (tenant_id, agent_app_id, version, kind, extension_name),
  KEY idx_ext_name (kind, extension_name)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='版本启用的 Plugin、Guardrail、Callback';
```

`kind` 决定挂载到框架的哪一类扩展位，`extension_name` 对应代码中注册的实现名。实现不入表，因此新增一种扩展只需注册实现，不改表结构也不改装配逻辑。

`priority` 用于确定同类扩展的执行顺序。多个 Guardrail 的先后次序会影响结果，例如脱敏应在审计记录之前，因此顺序需可配置而非依赖插入顺序。

### 5.6 agent_mcp_bindings

```sql
CREATE TABLE agent_mcp_bindings (
  id           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  tenant_id    VARCHAR(64)  NOT NULL,
  agent_app_id VARCHAR(64)  NOT NULL,
  version      VARCHAR(64)  NOT NULL,
  server_name  VARCHAR(128) NOT NULL COMMENT '关联 mcp_servers',
  enabled      TINYINT      NOT NULL DEFAULT 1,
  tool_filter  JSON         NULL COMMENT '仅暴露该 MCP 的部分 Tool 时使用',
  created_at   DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  PRIMARY KEY (id),
  UNIQUE KEY uk_mcp (tenant_id, agent_app_id, version, server_name)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='版本使用的 MCP 服务';
```

`tool_filter` 存在的原因是一个 MCP 服务通常暴露多个 Tool，而租户可能只希望开放其中部分。

### 5.7 agent_skill_bindings

```sql
CREATE TABLE agent_skill_bindings (
  id           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  tenant_id    VARCHAR(64)  NOT NULL,
  agent_app_id VARCHAR(64)  NOT NULL,
  version      VARCHAR(64)  NOT NULL,
  skill_name   VARCHAR(128) NOT NULL COMMENT '关联 skills',
  enabled      TINYINT      NOT NULL DEFAULT 1,
  params       JSON         NULL,
  created_at   DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  PRIMARY KEY (id),
  UNIQUE KEY uk_skill (tenant_id, agent_app_id, version, skill_name)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='版本挂载的 Skill';
```

### 5.8 agent_deployments

```sql
CREATE TABLE agent_deployments (
  id           BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  tenant_id    VARCHAR(64) NOT NULL,
  agent_app_id VARCHAR(64) NOT NULL,
  env          VARCHAR(32) NOT NULL DEFAULT 'prod',
  routes       JSON        NOT NULL COMMENT '[{"version":"v1","weight":90},{"version":"v2","weight":10}]',
  strategy     JSON        NULL COMMENT '预留：按租户、通道、用户或标签灰度',
  updated_by   VARCHAR(64) NULL,
  created_at   DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updated_at   DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (id),
  UNIQUE KEY uk_deploy (tenant_id, agent_app_id, env)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='环境发布与灰度权重';
```

一行代表一个环境，各版本权重存于 `routes`。调整权重与回滚均为单行更新，天然原子，不会出现权重之和不为 100 的中间态。若改为一行一个版本，调权重需要事务且存在中间态。

### 5.9 channel_bindings

```sql
CREATE TABLE channel_bindings (
  id                 BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  channel_binding_id VARCHAR(64)  NOT NULL,
  tenant_id          VARCHAR(64)  NOT NULL,
  agent_app_id       VARCHAR(64)  NOT NULL,
  env                VARCHAR(32)  NOT NULL DEFAULT 'prod',
  channel            VARCHAR(32)  NOT NULL COMMENT 'mock / wecom / wechat_kf / telegram',
  external_app_id    VARCHAR(128) NULL COMMENT '通道侧应用标识',
  webhook_path       VARCHAR(255) NULL,
  secret_ref         VARCHAR(255) NULL COMMENT '密钥引用，不存明文',
  capabilities       JSON         NULL COMMENT '能力描述符，结构见第 6 节',
  status             VARCHAR(32)  NOT NULL DEFAULT 'active',
  created_at         DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updated_at         DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (id),
  UNIQUE KEY uk_binding (channel_binding_id),
  UNIQUE KEY uk_webhook (webhook_path),
  KEY idx_tenant_app (tenant_id, agent_app_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='IM 账号与租户、Agent 绑定';
```

`webhook_path` 唯一，Gateway 据请求路径反查绑定，从而确定租户与 Agent。

### 5.10 sessions

```sql
CREATE TABLE sessions (
  id                 BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  session_id         VARCHAR(64)  NOT NULL,
  tenant_id          VARCHAR(64)  NOT NULL,
  agent_app_id       VARCHAR(64)  NOT NULL,
  agent_version      VARCHAR(64)  NOT NULL COMMENT '创建时固化，此后不随发布变化',
  channel            VARCHAR(32)  NOT NULL,
  channel_binding_id VARCHAR(64)  NOT NULL,
  scope              VARCHAR(16)  NOT NULL COMMENT 'single / group',
  scope_key          VARCHAR(128) NOT NULL COMMENT '单聊为 external_user_id，群聊为 external_group_id',
  internal_user_id   VARCHAR(64)  NULL COMMENT '群聊为空',
  last_sequence      BIGINT UNSIGNED NOT NULL DEFAULT 0,
  status             VARCHAR(32)  NOT NULL DEFAULT 'active',
  last_active_at     DATETIME(3)  NULL,
  created_at         DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updated_at         DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (id),
  UNIQUE KEY uk_session (session_id),
  UNIQUE KEY uk_scope (channel_binding_id, scope, scope_key),
  KEY idx_tenant_active (tenant_id, last_active_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='会话元数据';
```

`uk_scope` 直接对应会话定位规则：单聊由 `channel_binding_id + external_user_id` 定位，群聊由 `channel_binding_id + external_group_id` 定位，两者统一到 `scope` 与 `scope_key` 两列。

### 5.11 session_events

```sql
CREATE TABLE session_events (
  id            BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  tenant_id     VARCHAR(64) NOT NULL,
  session_id    VARCHAR(64) NOT NULL,
  sequence      BIGINT UNSIGNED NOT NULL COMMENT '会话内单调递增',
  event_type    VARCHAR(32) NOT NULL COMMENT 'user_message / agent_message / tool_call / tool_result / system',
  role          VARCHAR(16) NULL,
  content       JSON        NULL,
  request_id    VARCHAR(64) NOT NULL,
  trace_id      VARCHAR(64) NULL,
  agent_version VARCHAR(64) NULL,
  created_at    DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  PRIMARY KEY (id),
  UNIQUE KEY uk_seq (session_id, sequence),
  KEY idx_request (request_id),
  KEY idx_tenant_time (tenant_id, created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='会话事件';
```

`uk_seq` 是顺序正确性的硬保证：同一会话的相同 `sequence` 无法写入两次，重复执行或并发写入会在数据库层直接失败，而非产生错乱数据。

### 5.12 inbound_events

入站幂等记录。写入顺序为先落库、再回 ACK、最后入队，因此本表也是在途请求的唯一可靠记录。

```sql
CREATE TABLE inbound_events (
  id                 BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  tenant_id          VARCHAR(64)  NOT NULL,
  channel_binding_id VARCHAR(64)  NOT NULL,
  external_event_id  VARCHAR(128) NOT NULL COMMENT '通道侧事件或消息标识',
  request_id         VARCHAR(64)  NOT NULL,
  trace_id           VARCHAR(64)  NULL,
  session_id         VARCHAR(64)  NULL COMMENT '识别会话后回填',
  payload            JSON         NULL COMMENT '标准化后的统一消息，供重放使用',
  state              VARCHAR(32)  NOT NULL COMMENT 'processing / succeeded / delivery_failed / failed',
  attempts           INT          NOT NULL DEFAULT 0,
  last_error         VARCHAR(512) NULL,
  created_at         DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
  updated_at         DATETIME(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  PRIMARY KEY (id),
  UNIQUE KEY uk_event (channel_binding_id, external_event_id),
  KEY idx_state (state, updated_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='入站幂等记录';
```

`uk_event` 拦截通道的重复投递。`state` 区分执行与投递两个阶段，其中 `delivery_failed` 表示 Agent 已执行完成但回复未送达，该状态只重试投递，不重新执行 Agent 与 Tool。`idx_state` 用于扫描长时间停留在 `processing` 的记录，供对账与重放。

## 6. capabilities 结构

`channel_bindings.capabilities` 存放通道能力描述符，Gateway 与 Worker 据此决定行为，而非所有通道走同一路径。

```json
{
  "inbound_mode": "payload",
  "supports_push": true,
  "supports_edit": false,
  "max_text_length": 2048,
  "rate_limit_per_min": 20
}
```

| 字段 | 含义 |
|---|---|
| `inbound_mode` | 消息**怎么进来**：`payload` 回调自带内容 / `fetch` 回调仅通知需再拉取 / `stream` 由平台服务主动建长连接、服务端经该连接推送 |
| `outbound_mode` | 回复**怎么出去**：`direct` 任意 Worker 直接发（默认，留空即此值）/ `via_holder` 必须经持有该连接的进程发 |
| `supports_push` | 是否支持主动推送。ACK 后异步回复依赖此能力 |
| `supports_edit` | 是否支持编辑已发送消息 |
| `max_text_length` | 单条消息长度上限，超出需拆分 |
| `rate_limit_per_min` | 每分钟发送条数限制 |

**入站与出站是两个独立维度，不可互推。** 二者曾是一个字段，直到接第二个长连接通道才暴露问题：

| 通道 | 入站 | 出站 | 说明 |
|---|---|---|---|
| Mock / 企业微信回调 | `payload` | `direct` | |
| 企业微信 aibot | `stream` | `via_holder` | 回复须透传回调的 `req_id`，只有收到它的那条连接能做到 |
| Telegram long polling | `stream` | `direct` | 主动拉取入站，但回复走普通 HTTPS，任意 Worker 都能发 |
| 微信客服 | `fetch` | `direct` | |

企业微信 aibot 恰好两者耦合，所以一个字段曾能同时描述对。Telegram 打破了这个巧合：若沿用单字段，为表达「主动建连」而标 `stream`，会连带把回复路由进出站信箱并启动 per-bot 选主，而它两样都不需要。

`via_holder` 要求 `inbound_mode=stream`——没有持有的连接就无处可发，回复会进一个无人消费的信箱，用户静默收不到而其余信号全绿。Admin 接口会拒绝这种组合。

`stream` 模式的绑定不使用 `webhook_path`（没有回调地址），凭据也不同——存 BotID 与 Secret，而非 Token 与 EncodingAESKey，二者同样经 `secret_ref` 间接引用，不落明文列。

各通道的取值差异见 `IM通道接入设计.md` §7.1，长连接模式的完整设计见其 §9。

